NEXT_PUBLIC_SITE_URL=
GHL_WEBHOOK_URL=
# Unguessable slug for the private client dashboard at /d/<slug> — set in
# Vercel's project env vars for production, never committed. No value here
# means the route always 404s (see app/d/[slug]/page.tsx).
DASHBOARD_SLUG=
